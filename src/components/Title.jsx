import React from "react";

const Title = ({ text }) => {
  return (
    <h3 className="text-center text-2xl font-extrabold text-[#1A1919] md:text-5xl">
      {text}
    </h3>
  );
};

export default Title;
